name: C/C++ CI

on:
  push:
    branches: [ cbor ]
  pull_request:
    branches: [ cbor ]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: make check
      run: make -j check
    #- name: make distcheck
    #  run: make distcheck
